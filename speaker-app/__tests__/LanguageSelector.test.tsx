describe('LanguageSelector', () => {
  it('renders the list of languages in the modal', () => {
    expect(true).toBe(true);
  });
  it('saves the selected language in the global state', () => {
    expect(true).toBe(true);
  });
});
