describe('VocabularyList', () => {
  it('renders the list of words', () => {
    expect(true).toBe(true);
  });
  it('expands word cards to show phonetic transcription', () => {
    expect(true).toBe(true);
  });
});
