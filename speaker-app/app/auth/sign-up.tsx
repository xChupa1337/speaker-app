import React from 'react';
import EmailAuthFormScreen from "@/screens/email-auth-form-screen";

const SignUp = () => {
    return (
        <EmailAuthFormScreen isLogin={false} />
    );
};

export default SignUp;